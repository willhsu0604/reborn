package pe.servlet

import annotation.RestName
import pe.model.ProgramBean
import comm.HttpServletWrapper

@RestName("hi")
class HiServlet extends HttpServletWrapper {

//  override def doGet(e: ProgramBean): ProgramBean = {
//    new ProgramBean(e.name + "doGet:HiServlet", e.content, e.isEntryPoint)
//  }
//
//  override def doPost(e: ProgramBean): ProgramBean = {
//    new ProgramBean(e.name + "doPost:HiServlet", e.content, e.isEntryPoint)
//  }
//
//  override def doPut(e: ProgramBean): ProgramBean = {
//    new ProgramBean(e.name + "doPut:HiServlet", e.content, e.isEntryPoint)
//  }
//
//  override def doDelete(e: ProgramBean): ProgramBean = {
//    new ProgramBean(e.name + "doDelete:HiServlet", e.content, e.isEntryPoint)
//  }
}
